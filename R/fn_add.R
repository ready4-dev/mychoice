#' Add nothing
#' @description add_nothing() is an Add function that updates an object by adding data to that object. Specifically, this function implements an algorithm to add nothing. The function is called for its side effects and does not return a value.

#' @return NULL
#' @rdname add_nothing
#' @export 
add_nothing <- function () 
{
    print("this is a test")
}
